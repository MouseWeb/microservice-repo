spring:
  datasource:
    password: admin
    username: postgres
    url: jdbc:postgresql://localhost:5432/transportador
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update